<Window x:Class="WarehouseITSystem.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseITSystem"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="Система управления складом" Style="{StaticResource defaultWindow}" WindowStartupLocation="CenterScreen">
    <DockPanel>
        <TabControl x:Name="Items">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Style="{StaticResource defaultTextBlock}" Text="{Binding DisplayName}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
    </DockPanel>
</Window>
